version: "3.9"

services:
  m3u8_cache:
    image: ${IMAGE_NAME}
    container_name: m3u8_cache
    volumes:
      - /var/www/m3u8_cache/log:/app/log:rw
    restart: unless-stopped
    ports:
      - "0.0.0.0:8080:8080"
    env_file:
      - .env